{ ... }:

{
  # No settings yet -- the config file was empty before this migrated to
  # the home-manager module. Add `settings = { ... };` here as preferences
  # come up (see `ghostty +show-config --default` for available keys).
  programs.ghostty.enable = true;
}
